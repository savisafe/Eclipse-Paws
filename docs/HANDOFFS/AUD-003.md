# AUD-003 — Записать команды установки, запуска, build и test

- Статус: DONE (принято координатором 2026-09-05)
- Владелец: Claude
- Scope: `README.md` (был полностью пуст — обнаружено при выполнении задачи). Ноль изменений в
  коде.
- Изменённые файлы: `README.md`.
- Что реализовано: README теперь содержит требования (Node версия из `package.json` engines),
  install/dev/build/preview команды, полный список проверочных npm-скриптов
  (`lint`/`format:check`/`typecheck`/`test`/`test:e2e`/`check`), список dev-only debug
  query-параметров (`?level`, `?heroLevel`, `?phaseDurationMs`, `?checkpoint`,
  `?startNearCombat`/`?startNearFinish` — актуализировано вместе с `ARC-015`), и ссылки на
  остальную документацию (`docs/ECLIPSE_PAWS_SCENARIO.md`, `docs/ECLIPSE_PAWS_RECONSTRUCTION_PLAN.md`,
  `docs/AUDITS/`, `docs/HANDOFFS/`, `docs/ADDING_CONTENT.md`, `docs/ENVIRONMENT_ART_DIRECTION.md`),
  с явным примечанием, что README не является источником истины по сюжету/архитектуре (план §2).
- Что намеренно не реализовано: не описывал CI-конфигурацию (её нет в репозитории на данный
  момент) и не добавлял badges/лицензию — вне scope задачи "записать команды".
- Изменённые контракты: нет — документация.
- Проверки и результаты: `npm run format:check` — чисто. Остальные проверки не относятся к
  документации, но прогнаны в рамках общей сессии (см. handoff `ARC-015`).
- Ручная проверка: каждая перечисленная команда сверена с реальным `package.json`/`playwright.config.ts`.
- Известные ограничения: нет.
- Рекомендованный следующий task ID: `ARC-015` (debug launcher) — выполнен в этой же сессии сразу
  после этой задачи, README уже обновлён под него.
